# Cloudflare Realtime TURN credentials
# 1. Go to dash.cloudflare.com → Media → Calls → TURN Keys
# 2. Create a TURN key — copy the Key ID and API Token
# 3. Add both values here and in your Vercel project environment variables
CLOUDFLARE_TURN_TOKEN_ID=
CLOUDFLARE_API_TOKEN=
